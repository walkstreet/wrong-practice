# MVP 与最终版 GAP 记录

## 1. 当前 MVP 已完成

- 后端框架与基础结构：`FastAPI + SQLAlchemy + SQLite`
- 错题核心能力：新增、编辑、删除（软删）、详情、分页与筛选
- 分类能力：题型/知识点基础管理接口
- Dify 集成能力（简化鉴权）：`X-API-Key` 读取错题与回写练习记录
- 练习记录：持久化入库 + 列表查询 + 错题正确率统计
- Web 管理界面（轻量 HTML）：
  - 错题列表与筛选
  - 错题详情
  - 手动新增
  - 练习记录与统计

## 2. 与最终版的主要 GAP

### 2.1 OCR 能力（当前为占位流程）

- 现状：`/api/v1/wrong-questions/ocr` 需要外部传入 `extracted` 结构化结果
- 目标：服务端真实接入 OCR 引擎（图片上传 -> OCR -> 字段抽取 -> 人工校对 -> 入库）
- 影响：这是业务主入口，优先级高

### 2.2 鉴权与安全（当前偏 MVP）

- 现状：仅 `X-API-Key`
- 目标：签名校验（HMAC）、时间戳、防重放、幂等键、访问审计
- 影响：生产可用性与安全合规

### 2.3 数据库工程化

- 现状：`create_all` 自动建表，无迁移版本控制
- 目标：引入 Alembic 管理 schema 迁移
- 影响：多环境部署和持续迭代稳定性

### 2.4 管理端体验

- 现状：服务端拼接 HTML，功能可用但体验基础
- 目标：升级为前后端分离管理台（如 React/Vue），支持更丰富交互
- 影响：效率与可维护性

### 2.5 统计能力深度

- 现状：按错题聚合正确率 + 简单列表
- 目标：
  - 时间维度趋势（7/30 天）
  - 按题型/知识点统计
  - 个人/班级维度（若做多用户）
- 影响：学习反馈与策略优化

### 2.6 质量保障

- 现状：已做语法检查，缺少自动化测试
- 目标：单测 + API 集成测试 + 基础 CI
- 影响：迭代稳定性

### 2.7 部署与运行

- 现状：本地开发启动
- 目标：容器化、环境配置分层、日志与监控、备份策略
- 影响：上线与运维

## 3. 建议迭代路线（按优先级）

### P0（尽快）

1. 真实 OCR 接入（至少可用一条稳定 pipeline）
2. Alembic 迁移管理
3. Dify 接口签名与幂等

### P1（下一阶段）

1. 完善统计看板（趋势/分布）
2. PracticeRecord 维度扩展（题型、知识点、时间区间）
3. 基础测试体系（核心 API 覆盖）

### P2（产品化）

1. 管理端前后端分离
2. 多用户与权限模型（RBAC）
3. 可观测性完善（监控、告警、审计）

## 4. 推荐的“最终版”能力清单（目标态）

- 稳定 OCR 入库链路（可回溯、可重试）
- 完整 Dify 集成（读取 + 回写 + 安全签名）
- 可视化管理台（错题、分类、统计、练习反馈）
- 生产级工程体系（迁移、测试、CI/CD、监控）
